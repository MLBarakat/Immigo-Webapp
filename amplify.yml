version: 1
backend:
  phases:
    build:
      commands:
        - '# Execute Amplify-provided script to detect and configure backend.'
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - '# Install frontend dependencies'
        - npm install
        - '# Install backend dependencies'
        - (cd server && npm install)
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - server/node_modules/**/*